# Free, instant, no card: https://fredaccount.stlouisfed.org/apikeys
FRED_API_KEY=

# https://console.anthropic.com  -> Settings -> API Keys
# Only needed for the narration layer (M2). Ingest and modelling run without it.
ANTHROPIC_API_KEY=

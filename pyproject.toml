[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "calibrated-equity-forecast"
version = "0.1.0"
description = "Calibrated selective prediction for next-session equity direction: evidence from the NSE"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["cef*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

# Slim dependency set for GitHub Actions.
#
# The full requirements.txt pins torch and transformers for the FinBERT
# sentiment model. That import is lazy and lives in cef/evidence/sentiment.py,
# which no CI job touches — but installing it anyway pulls torch's CUDA wheels
# on Linux, several gigabytes for code that never runs. Verified: the whole
# track path (service, dashboard, tracking, predict, ensemble) imports with
# only what is listed here.
#
# Keep the pins matching requirements.txt so CI and local agree.
numpy==2.2.6
pandas==2.3.3
pyarrow==25.0.1
scikit-learn==1.7.2
scipy==1.15.3
lightgbm==4.7.0
shap==0.49.1
yfinance==1.7.0
requests==2.32.5
python-dotenv==1.2.1
feedparser==6.0.14
